import React from "react";

function AwardComponent({ image, price, styles }) {
  return;
  <div>
    <img src={image} alt={price} className={styles}></img>
    <span></span>
  </div>;
}

export default AwardComponent;
